module UIObjects
{
    export class Button extends createjs.Bitmap
    {
        // PRIVATE FIELDS (CLASS MEMBERS)

        // PUBLIC PROPERTIES

        // CONSTRUCTOR(S)
        constructor()
        {
            super()
        }
        // PRIVATE METHOD(S)

        // PUBLIC METHOD(S)
    }
}